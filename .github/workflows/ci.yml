name : MYCI

on :
  push : 
    branches: [main]

jobs :
  Build :
    runs-on: "ubuntu-latest"

    steps:
      - name : Checkout
        uses : actions/checkout@v3
      - name : BuildPython
        uses : actions/setup-python@v4
        with :
          python-version : "3.10"
      - name : Install Dependecies
        run : |
          if [-f code.py] ;then
            echo "fil found"
          elif [-f jibran.txt]; then ;
            echo "file founded"
          else  
              echo "not found"
          fi 
      - name : Greet 
        run : |
          echo "hi"
