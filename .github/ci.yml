name : MyCI
on :
  push :
    branches : [main]


jobs :
  build :
    runs-on : ubuntu-latest

    steps :
      - name : checkoutCode
        uses : actions/checkout@v3

      - name : BuildPython
        uses : actions/setup-python@v4
        with :
          python-version : "3.10"
        
      - name : RunScript
        run : |
          echo "Heloo"